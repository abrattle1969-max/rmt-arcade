# RMT Arcade

Static campaign-game lobby for the RMT Arcade.

Render settings:

- Runtime: Static Site
- Build command: blank
- Publish directory: `.`

The first release links to the existing production game builds. Each game can later be migrated into `games/<slug>/` without changing the lobby design.
